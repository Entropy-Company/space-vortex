<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:co="clr-namespace:Content.Client.UserInterface.Controls"
    MinHeight="600"
    MinWidth="450">
    <BoxContainer Name="MainContainer" Orientation="Vertical">
        <LineEdit Name="SearchBar" PlaceHolder="{Loc 'vending-machine-component-search-filter'}" HorizontalExpand="True"  Margin ="4 4"/>
        <!-- Economy-Start -->
        <BoxContainer Orientation="Horizontal" HorizontalExpand="True" MinSize="0 54" Margin="4 2 4 2" StyleClasses="windowBackground Panel">
            <EntityPrototypeView
                Name="CreditsIcon"
                Access="Public"
                MinSize="32 32"
                MaxSize="32 32"
                Margin="4 0 4 0" />
            <Label Name="CreditsLabel" HorizontalExpand="True" VerticalAlignment="Center"/>
            <Button
                Name="WithdrawButton"
                Text="{Loc 'store-ui-default-withdraw-text'}"
                MinSize="70 25"
                Margin="0 0 4 0"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"/>
        </BoxContainer>
        <!-- Economy-End -->
        <co:SearchListContainer Name="VendingContents" VerticalExpand="True" Margin="4 0"/>
         <!-- Footer -->
        <BoxContainer Orientation="Vertical">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <Label Text="{Loc 'vending-machine-flavor-left'}" StyleClasses="WindowFooterText" />
                <Label Text="{Loc 'vending-machine-flavor-right'}" StyleClasses="WindowFooterText"
                        HorizontalAlignment="Right" HorizontalExpand="True"  Margin="0 0 5 0" />
                <TextureRect StyleClasses="NTLogoDark" Stretch="KeepAspectCentered"
                        VerticalAlignment="Center" HorizontalAlignment="Right" SetSize="19 19"/>
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>